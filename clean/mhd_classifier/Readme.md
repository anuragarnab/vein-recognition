MHD Classifier
===

Please run "demo.m" for an overview of the MHD Classifier
If you want to populate the training database yourself (and pre-process all the images), run demo_extended.

If so, please modify the path of the SDUMLA-HMT database in "load_data.m" (It only uses absolute paths to find the database)