% Im just doing this from here so that I can use the "Run and Time" button
[distances_mhd_rel, imp_distances_mhd_rel] = distance_matrix(veins, veins_unreg, 0.2);

% for i = 1:10
%    d = mhd_fast (veins(i).distances, veins(77).distances); 
% end